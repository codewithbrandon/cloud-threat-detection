# Core application dependencies
flask==3.0.3
gunicorn==22.0.0

# Prometheus metrics instrumentation
prometheus-client==0.20.0

# HTTP client for attack simulation scripts
requests==2.32.3

# Structured logging
python-json-logger==2.0.7

# Security hardening — prevent YAML deserialization attacks
PyYAML==6.0.1

# Timezone support
pytz==2024.1
